{
  "name": "@pleiad/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed, platform-agnostic Pleiad API client (pure fetch + zod) shared by the mobile and web apps. Consumed as TypeScript source — no build step.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "dependencies": {
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.0.17"
  }
}
