{
  "name": "@omnis/scraper",
  "version": "1.0.0",
  "private": true,
  "description": "Knowledge-base ingestion pipeline: /learn docs corpus -> chunks -> bge-small embeddings -> Postgres (pgvector)",
  "type": "module",
  "scripts": {
    "ingest": "npx tsx src/ingest.ts",
    "verify": "npx tsx src/verify.ts"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
